Capital Markets Desk — v4.4.4 — fast_info degradation fix (the probes' verdict)
Changed files (full replacements):

desk/flow.py  ::  """Flow layer — the Sector Flow Tracker's automatable half, in-app
desk/instflow.py  ::  """Institutional flow — where big players can't hide.
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL 

PROBE VERDICT (24-Jul strip): fast_info responds but returns NONE
for shares (flows) and last_price (OI spot) under the yfinance
version the 4.x requirements installed — both accrual failures, one
root. FIX: flows ladder gains info[sharesOutstanding] before
get_shares_full, price falls to history close; oi_snapshot spot
falls to history close when fast_info yields nothing. Both paths
unit-tested with fast_info-returns-None mocks (23/23 ETFs resolve;
OI band filters correctly off history spot). After deploy, reload
Summary: APP-ACCRUAL should print logged 23 and stored. 4.4.2/4.4.3
probes remain in place — empties keep naming their causes.
theme.VERSION 4.4.4.

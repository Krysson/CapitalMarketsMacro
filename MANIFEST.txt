Capital Markets Desk — v3.15.0 — FINRA ATS dark-venue layer live
Changed files (full replacements; paste at these paths):

desk/instflow.py  ::  """Institutional flow — where big players can't hide.
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL EDITION.
pages/17_Flow.py  ::  """Flow Desk — the Sector Flow Tracker's automatable half, live.
pages/16_Help.py  ::  """Help — the desk's function reference and operating manual.
README.md  ::  # Capital Markets Desk (Streamlit)

NEW: FINRA ATS weekly data on the Flow page — OAuth2 token exchange,
weeklySummary dataset filtered to ATS_W_SMBL + the desk's ETF set,
casing-tolerant parser (spec versions differ), per-symbol latest-week
table (ATS shares, trades, SHARES/TRADE) and a concentration flag when
a symbol's latest shares-per-trade runs >=1.25x its trailing average.
Delayed 2 weeks by rule; the note explains why that costs little at
the accumulation timescale. SETUP: add FINRA_API_CLIENT_ID and
FINRA_API_SECRET to the Streamlit app secrets (README section updated;
Help keys table updated). Without credentials the panel shows a setup
note and nothing else changes. theme.VERSION 3.15.0.

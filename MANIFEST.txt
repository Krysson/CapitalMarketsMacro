Capital Markets Desk — v3.9.1 — promote-prefill fix + scrollbar fix + publish guard
Changed files (full replacements; paste into GitHub at these paths):

desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL EDITION.
pages/4_Notebook.py  ::  """Analyst's Notebook — Evidence → Interpretation → Risks → Falsification."""
README.md  ::  # Capital Markets Desk (Streamlit)

FIX 1: promoted jots published blank — prefill via value= broke on the
submit rerun (widget auto-ID change). Evidence now uses a stable key
seeded before the form; repeat promotes APPEND. FIX 2: st.iframe grew
scrollbars on every TradingView widget — embed() now injects a
margin/overflow reset into the iframe document. NEW: blank entries
(no Evidence and no Decision) can no longer be published — saved to
the scratchpad with an error instead. Instrument input normalized
(dji -> ^DJI, spx -> ^GSPC, etc.) so post-mortem grading can fetch it.
theme.VERSION 3.9.1. Both gotchas added to the README.

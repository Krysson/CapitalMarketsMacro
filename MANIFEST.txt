Capital Markets Desk — v4.5.1 — keyboard capture, self-reporting
Changed file: desk/theme.py (full replacement)

The injector now prints its own status in tiny gray text under the
command bar: KEYS: armed (green, working) / blocked — sandboxed
iframe (red — Streamlit denies parent access and the feature is
infeasible on this platform) / command bar not found (yellow —
selector fix). Whatever it says IS the diagnosis; report the exact
wording. If it says armed and typing still does nothing, report
that too — different fix (event timing). theme.VERSION 4.5.1.

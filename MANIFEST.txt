Capital Markets Desk — v4.7.3 — the wire blocks render (src vs source)
Changed files: desk/analyst.py, desk/theme.py (full replacements)

THE GLASS CONVICTED THE CODE: fetch_tape items carry the key 'src';
the v4.7 wire blocks asked for 'source', raised KeyError on the
first item, and the fail-soft wrapper silently ate both [T1] and
[T3] — which is why the snapshot showed only the original PRIMARY
WIRE line (it reads 'title' only). Key accesses fixed. After
deploy, open WHAT THE ANALYST SEES: both tiers should render.
theme.VERSION 4.7.3.

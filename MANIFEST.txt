Capital Markets Desk — v4.4.7 — THE WALLS panel + breadth-after-exit fix
Changed files (full replacements):

scripts/snapshot.py  ::  breadth job now a function called BEFORE SystemExit
pages/3_Volatility.py  ::  THE WALLS — put/call OI by strike from oi_latest
desk/theme.py  ::  4.4.7

BREADTH ROOT CAUSE (24-Jul log, zero breadth output): the job was
appended AFTER raise SystemExit(main()) — code after an exit never
runs. Tonight expects: breadth: 503 constituents ... wrote ~250 rows.
THE WALLS (Vol page): 'read the chain' pointed at furniture that
didn't exist — now the desk's own nightly OI record renders as
put/call shelves by strike with a spot line, PUT/CALL WALL readout
with distances, and honest tiering (T1 OI, T3 dealer inference —
walls are zones, not laws; cross-check footprints for shelves under
construction). theme.VERSION 4.4.7.

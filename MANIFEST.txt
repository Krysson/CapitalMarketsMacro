Capital Markets Desk — v3.8.0 — Notebook publishing (the public record)
Changed/new files (full replacements; paste into GitHub at these paths):

desk/publish.py  ::  """Publisher for Notebook entries — the desk's public record.
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL EDITION.
pages/4_Notebook.py  ::  """Analyst's Notebook — Evidence → Interpretation → Risks → Falsification."""
README.md  ::  # Capital Markets Desk (Streamlit)

New since 3.7.0: desk/publish.py (GitHub contents API, data branch,
fail-soft); per-entry publish checkbox in the Notebook (scratchpad stays
private by default); post-mortem grades mirrored to the published file;
'The public record' browser on the Notebook page; theme.VERSION 3.8.0.
ONE-TIME SETUP: fine-grained PAT scoped to ONLY this repo with
Contents: Read and write -> Streamlit app secrets as GH_TOKEN.
(README section: 'Publishing Notebook entries'.) Requires the data
branch from the snapshot workflow to exist (it does).

Capital Markets Desk — v3.17.0 — SKEW lives · quote fallback ladder · red negatives
Changed files (full replacements; paste at these paths):

desk/data.py  ::  """Data layer for the Capital Markets Desk.
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL EDIT
pages/3_Volatility.py  ::  """Volatility Dashboard — Book III, Ch. 6: the observable vol complex.
pages/6_Quote.py  ::  """Quote — security & series lookup. The desk's DES / GP / FA.
pages/17_Flow.py  ::  """Flow Desk — the Sector Flow Tracker's automatable half, live.
pages/14_History.py  ::  """Regime History — the desk's live-accrued track record."""
pages/7_Rates.py  ::  """Rates & Credit — the free window into fixed income. GC <GO>.
README.md  ::  # Capital Markets Desk (Streamlit)

SKEW: Yahoo dropped Cboe's indices — data.cboe_series() reads the
index owner's own CDN (keyless, T1). Vol-page spark restored; full
SKEW panel with 2y chart, 1y-percentile readout (>=85th = 'tail
hedgers haven't signed off' framing vs PCC), and the honest caveat
that Cboe's announced methodology revision will break comparability
when it lands. SKEW / VVIX are quote commands (Cboe mode).
QUOTE FALLBACK LADDER: unknown ticker -> Stooq computable fallback
[T2] -> TradingView symbol widget (display glass, symbol box open).
RED NEGATIVES: theme.neg_red() house helper; applied to Flow rotation
+ streaks, History track record, Rates auctions, Quote financials —
chain onto any future styled table. theme.VERSION 3.17.0. No new keys.

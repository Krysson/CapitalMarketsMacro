Capital Markets Desk — v4.7.1 — Analyst narrative wire fix
Changed files: desk/analyst.py, desk/theme.py (full replacements)

The v4.7.0 snapshot fetched only the Google narrative lanes and
swallowed any failure silently — the Analyst saw [T1] but not [T3]
and honestly said so. Now it fetches the SAME set the Wire page
renders (NARRATIVE_FEEDS + GOOGLE_NARRATIVE_FEEDS, so CNBC/
MarketWatch/Google lanes incl. G-OIL — the Iran/oil narrative
lane), takes 8 headlines, and when the narrative tape is genuinely
unavailable the snapshot SAYS SO with the exception name instead
of omitting the block (v4.4.5 house rule, applied to the machine's
own context). Test: ask the Analyst what is on the narrative wire.
theme.VERSION 4.7.1.

Capital Markets Desk — v4.4.5 — Buffett fallback · explained empties · strip ordering
Changed files (full replacements):

pages/20_Valuation.py  ::  """Valuation (VAL) — the horizon anchor. Valuation is a HORIZON to
pages/0_Summary.py  ::  """Capital Markets Desk — Summary page (the desk's front door)."""
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL 

BUFFETT INDICATOR: FRED WILL5000PR died when Wilshire ended the
partnership (2024) — the page now falls to Yahoo ^W5000 [T2],
labeled, and when BOTH sources are empty the panel SAYS SO instead
of vanishing (new house rule: empty panels explain themselves —
silent absence is what cost us this week). STRIP ORDERING: the
app accrual now runs BEFORE the session counts, so the bot strip
reads post-accrual state — no more "flows 0 sessions" next to
"logged 20" from the same page load. theme.VERSION 4.4.5.

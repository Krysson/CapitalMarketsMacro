Capital Markets Desk — v4.1.2 — month-contract futures fix (Paper Desk P&L)
Changed files (full replacements; paste at these paths):

desk/paper.py  ::  """Paper engine — the desk's own simulator, every asset class it wat
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL ED
README.md  ::  # Capital Markets Desk (Streamlit)

FIX: Yahoo month contracts (CLV26.NYM, ESZ26.CME — exchange suffixes
.NYM/.CME/.CBT/.CMX/.NYB with month-code+year) were classified as
EQUITIES: multiplier 1, bps slippage, cash debited — a real -$252
printed as -$0. Now parsed to their root and inheriting its spec
(CLV26 -> CL: $1000/pt, 1-tick slip, no cash debit). Unknown roots
REFUSE the order rather than fill at a fake 1x. USER ACTION: close
and re-enter any month-contract positions opened before this fix —
stored positions keep their opening terms (impact: pennies).
theme.VERSION 4.1.2.

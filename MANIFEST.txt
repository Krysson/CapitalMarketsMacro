Capital Markets Desk — v4.5.2 — keyboard capture: live event echo
Changed file: desk/theme.py (full replacement)

The status line now ECHOES every keystroke the listener hears:
'KEYS: armed · heard 3 (v)'. Deploy, click empty page background
once, type a letter, watch the line. IF THE COUNTER MOVES but the
bar stays empty -> events arrive, insertion fails (fixable). IF IT
NEVER MOVES -> keystrokes aren't reaching the app document; the
capture surface is wrong and likely unfixable on this platform.
Also listens on the window as a second surface (deduped). Report
the exact line text after typing 2-3 keys. theme.VERSION 4.5.2.

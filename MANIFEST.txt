CAPITAL MARKETS DESK — v4.9.2 (THE HONEST ZERO)
August 6, 2026

Baseline: v4.9.1 — apply 4.9.0 → 4.9.1 → this, in order. Four
replacement files, drop-in at repo paths. This is the pre-freeze
fix pass from live QA of 4.9.1.

ROOT CAUSE FIXED — STATIC SHARES SOURCE
  Ten accrued sessions showed byte-identical shares outstanding for
  every fund (SPY at exactly 917,782,016 for two weeks — impossible;
  SPY creates/redeems daily). Yahoo's quote-endpoint shares figure
  is a rarely-refreshed DISPLAY number, and the snapshot ladder
  treated its answer as success, so the fundamentals timeseries that
  actually updates (get_shares_full) never ran. Every flow has been
  structurally $0 since accrual start — the blank charts, the
  "$+0mm no rotation signature" line, and "GLD +0.00%" were
  arithmetic on a dead input.

FILES
  desk/flow.py      — snapshot ladder INVERTED: get_shares_full
                      (the series Yahoo updates) runs first; quote
                      figures are the fallback. New flows_static():
                      record old enough to judge + every flow
                      exactly zero = dead input, not a quiet market.
  desk/accrue.py    — OI same-day HEAL: a record stored degraded at
                      7am retries on later loads (OI often populates
                      mid-morning); the zero-OI guard still refuses
                      to replace healthy with zeros. Footprint diffs
                      are BLOCKED against a degraded prior record —
                      oi_prev=0 would mint the entire OI as a fake
                      overnight footprint.
  pages/17_Flow.py  — TWO-COLUMN REFLOW: row 1 = rotation monitor |
                      short-volume; row 2 = dark venues | ETF flow
                      record; scoreboard, footprints, and the expiry
                      cut stay full-width. Scoreboard gains its
                      explanation note (all four chips defined, incl.
                      what "record n=X" means). ETF section now
                      detects the static-source state and replaces
                      blank charts and misleading $0 readouts with a
                      named SOURCE DEGRADED explanation + the
                      post-mortem note; scoreboard ETF chip shows
                      "source static" in red. z-scores render as
                      +x.x or an em-dash, never "None".
  desk/theme.py     — VERSION 4.9.2.

TESTS (pre-package)
  ast + imports — 4 files: PASS. flows_static returns TRUE on the
  ACTUAL dead flows.csv from the data branch, FALSE on the same
  record with one real print added, FALSE on thin/empty. Ladder
  order verified at the call-site level (get_shares_full precedes
  fast_info). Heal retry fires on the actual degraded 06-Aug OI
  record, not on healthy same-day; degraded-prev footprint diff
  blocked. z formatting exact. Reflow verified on unique anchors
  with every v4.9.1 feature (scoreboard, measurement pass, vs-4wk,
  CAMP, expiry cut, VS OWN RECORD) intact post-indent.

WHAT TO EXPECT ON DEPLOY
  Flow page: SOURCE DEGRADED panel where the blank charts were —
  correct until the first accrual with the fixed ladder writes a
  session with real share deltas; flows then compute forward from
  that session (the zero rows stay as an honest scar). Vol page:
  WALLS WITHHELD stands until a healthy OI snapshot; with the heal
  fix, that can now happen the SAME DAY on any app load once Yahoo
  populates OI. If the walls are back by your afternoon check, the
  heal worked.

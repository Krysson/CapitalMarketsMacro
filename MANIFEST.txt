Capital Markets Desk — v4.4.9 — last-price line, bar-direction colored
Changed files (full replacements):

desk/theme.py  ::  last_price_line() helper + 4.4.9
pages/6_Quote.py  ::  line on the ticker chart (all D/W/M intervals)
pages/2_Market.py  ::  line on the SPX trend chart

TradingView-style dotted line at the last close with an axis-side
price label — GREEN if the displayed bar is up vs the prior bar,
RED if down, and INTERVAL-AWARE because it reads the resampled bars
on the chart: daily view colors by the day, weekly by the week,
monthly by the month. Title last-print retained. theme.VERSION 4.4.9.

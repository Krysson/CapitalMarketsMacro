Capital Markets Desk — v4.4.6 — Live SPX first (guarded) + MA Ribbon preloaded
Changed files (full replacements):

pages/2_Market.py  ::  """Market Dashboard — Book III, Ch. 5: trend, participation, cro
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINA

TAB ORDER: Live SPX restored as the default tab, now with a guard
comment so no future build reverts it (root cause: v4.4.0 shipped
this page from a local copy that predated the manual swap — my
drift, my fix). MA RIBBON: the TradingView embed now requests the
MA Ribbon study on load (studies param). CAVEAT: study IDs on the
free embed are finicky — if the ribbon does not render after
deploy, the widget silently ignores unknown IDs (chart still
works); report and we try the alternate ID form. True per-user
persistence of widget settings is not possible in an anonymous
embed — preloading via config is the only sticky mechanism.
theme.VERSION 4.4.6.

Capital Markets Desk — v4.4.2 — accrual diagnostic probes (2 files; supersedes the earlier 3-file 4.4.2 if downloaded)
Changed files (full replacements):

desk/accrue.py  ::  """App-side accrual (v4.4.1) — Plan B, now the plan.
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL 

This morning's APP-ACCRUAL reported "no rows (Yahoo?)" — but the
app's network provably works, so SOMETHING ELSE failed inside the
per-ticker handlers, and fail-soft swallowed the evidence. This
patch makes the accrual PROBE one live call whenever it comes up
empty and print the actual exception type + message on the bot
strip (e.g. "probe AttributeError: ..." or "probe shares=None").
Deploy, reload Summary once, and read the strip: the next status
line IS the diagnosis. theme.VERSION 4.4.2.

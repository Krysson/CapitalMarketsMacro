Capital Markets Desk — v4.6.1 — palette chord: Ctrl+Space (Ctrl+K was browser-reserved)
Changed file: desk/theme.py (full replacement)

Ctrl+K never reached the page — Chrome/Edge claim it for the
address bar. Streamlit's validator rejects punctuation chords
(Ctrl+/ and Ctrl+. both refused), leaving Ctrl+Space / Alt+K /
Ctrl+M; Ctrl+Space wins (unclaimed by Chrome/Edge; Ctrl+M mutes
tabs in some browsers, Alt chords hit menu accelerators). If
Ctrl+Space collides with an IME or your muscle memory disagrees,
it is a one-word edit: shortcut='Alt+K' on the palette_btn line.
theme.VERSION 4.6.1.

Capital Markets Desk — v4.4.1 — APP-SIDE ACCRUAL (Plan B is the plan)
Changed/new files (full replacements):

desk/accrue.py  ::  """App-side accrual (v4.4.1) — Plan B, now the plan.
desk/theme.py  ::  """Shared visual identity for the Capital Markets Desk — TERMINAL 
pages/0_Summary.py  ::  """Capital Markets Desk — Summary page (the desk's front door)."""
README.md  ::  # Capital Markets Desk (Streamlit)

VERDICT after two nights of runner logs: Yahoo hard-blocks the
quote/fundamentals/options endpoints from GitHub IPs even with
impersonation (chart endpoint passes — signals row proves it). NEW
desk/accrue.py: when the Summary page loads and yesterday's flow
rows (or today's OI snapshot) are missing from the data branch, the
APP fetches them — its IPs work, proven daily by the rotation
monitor and skew curve — and commits via the Notebook's GH_TOKEN
machinery. Throttled (4h cache), fail-soft, status reported on the
bot strip as APP-ACCRUAL. Morning OI accrual is better data anyway:
chains report refreshed OI overnight. Gaps close at next open.
REQUIRES: GH_TOKEN already in app secrets (it is — Notebook publish
works). The nightly bot continues unchanged for signals + breadth.
theme.VERSION 4.4.1.

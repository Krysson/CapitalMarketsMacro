Capital Markets Desk — v4.6.2 — palette autofocus
Changed file: desk/theme.py (full replacement)

st.dialog does not focus its first input and text_input has no
autofocus param, so a 12-line script inside the dialog finds its
own input in the parent DOM and focuses it (polling up to 1.5s for
render). Odds are good: the keyboard saga proved DOM ACCESS works
from components here — only event listening was blocked. Fail-soft:
if the extension lockdown blocks this too, behavior reverts to
exactly today's (one click), never worse. Test: Ctrl+Space, type
immediately without clicking. theme.VERSION 4.6.2.

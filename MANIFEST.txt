Capital Markets Desk — v4.5.3 — keyboard capture: final form (supersedes 4.5.0-4.5.2)
Changed file: desk/theme.py (full replacement)

INVISIBLE: the status line is gone — diagnostics now print to the
browser console (F12) as [desk keys] lines. DESKTOP-ONLY: touch
devices skip capture entirely (soft keyboards have nothing to
capture; the mobile clutter line is gone). RACE FIXED: the bar is
looked up per keystroke, never only at load. HOTKEY DUEL: capture
runs in the capture phase with stopPropagation, so typing CRACK no
longer summons Streamlit's clear-cache dialog on the letter c —
and c/r remain Streamlit hotkeys only when the desk has no bar to
feed. DESKTOP TEST: click empty background, type VOL — chars
should land in the bar; if not, F12 console shows [desk keys]
lines that name the failure. theme.VERSION 4.5.3.
